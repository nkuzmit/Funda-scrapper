telegram:
  bot_token: "YOUR_TELEGRAM_BOT_TOKEN"
  chat_id: "YOUR_TELEGRAM_CHAT_ID"

filters:
  areas:
    - "amsterdam"            # list of funda area slugs or cities
  price_min: 0
  price_max: null            # null = no upper bound
  publication_days: null     # only listings published within this many days
  energy_labels: []          # e.g. ["A+++", "A++"]
  min_bedrooms: 1
  keywords: []

schedule:
  times_per_day: 4
  hours: [9, 12, 15, 18]